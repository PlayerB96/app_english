# Plantillas de referencia por servidor (sin secretos).
#
# Copia los valores de DB_HOST y APP_URL al .env local de cada nodo.
# El .env real no se commitea; estos archivos solo documentan qué cambia por servidor.
#
# Variables compartidas: ver .env.example en la raíz del proyecto.
