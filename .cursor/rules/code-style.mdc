---
description: Reglas de estilo de código para Laravel 12 + Vue 3
globs: ["**/*"]
---

# Estilo de código

## PHP / Laravel

### Controladores
- Usar Form Requests para toda validación de entrada
- Usar API Resources para toda transformación de respuesta
- Delegar lógica de negocio a Services, no poner lógica en Controllers
- Un método público por acción; preferir Resource Controllers (index, store, show, update, destroy)

### Services
- Una clase por dominio de negocio (ej: `ProductService`, `SaleService`)
- Inyección de dependencias por constructor
- Retornar DTOs o modelos, nunca arrays genéricos
- Manejar excepciones de negocio con excepciones custom

### Repositories
- Encapsulan todas las queries Eloquent
- Interfaz + implementación para facilitar testing
- Los Services consumen Repositories, nunca acceden a Eloquent directamente

### Modelos
- Definir `$fillable` explícitamente, nunca usar `$guarded = []`
- Definir casts para tipos complejos
- Relaciones tipadas con return types
- Scopes para queries reutilizables

### Validación
- Form Requests para toda validación
- Mensajes de error en español configurados en `lang/es/validation.php`
- Rules personalizadas en `app/Rules/`

### Migraciones
- Nombres descriptivos (ej: `create_products_table`, `add_barcode_to_products`)
- Siempre definir `down()` para rollback
- Foreign keys explícitas con `constrained()` y `cascadeOnDelete()` donde aplique

## Vue 3 / TypeScript

### Componentes
- Composition API con `<script setup lang="ts">` obligatorio
- Props tipadas con `defineProps<{}>()` (no runtime props)
- Emits tipados con `defineEmits<{}>()` 
- Un componente por archivo, nombrado en PascalCase

### Estado
- Pinia para estado global (autenticación, carrito, sesión de venta)
- `ref()` / `reactive()` para estado local del componente
- Composables para lógica reutilizable entre componentes

### Estilos
- Tailwind CSS como framework principal
- No usar `<style scoped>` salvo excepciones justificadas
- Clases utilitarias de Tailwind en el template

### TypeScript
- Strict mode habilitado
- No usar `any` — usar `unknown` si el tipo no se conoce
- Interfaces para contratos de datos (props, API responses)
- Types para uniones y tipos derivados
- Archivos de tipos en `resources/js/types/`
